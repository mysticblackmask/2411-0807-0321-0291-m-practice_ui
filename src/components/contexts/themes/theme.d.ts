export interface themeType {
  back: string;
  badge: string;
}

export interface clrType {
  primary: themeType;
  error: themeType;
  info: themeType;
  secondary: themeType;
  success: themeType;
  warning: themeType;
}
